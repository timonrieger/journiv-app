# Test dependencies
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-httpx==0.36.0

# HTTP testing
httpx==0.28.1
requests==2.32.5 # For upgrade tests (external HTTP calls)

# Database testing - using psycopg2-binary for compatibility
psycopg2-binary==2.9.11

# Authentication testing - ensure argon2 backend is available
argon2-cffi==25.1.0
