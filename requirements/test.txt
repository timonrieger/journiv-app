# Test dependencies
pytest==8.3.5
pytest-asyncio==0.25.2
pytest-cov==5.0.0
pytest-mock==3.15.1
pytest-httpx==0.35.0

# HTTP testing
httpx==0.28.1
requests==2.32.3  # For upgrade tests (external HTTP calls)

# Database testing - using psycopg2-binary for compatibility
psycopg2-binary==2.9.11

# Authentication testing - ensure argon2 backend is available
argon2-cffi==25.1.0
